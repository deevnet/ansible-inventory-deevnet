---
# Topic permissions for the MQTT broker.
#
# Kept in inventory rather than the role so that adding a device is an
# inventory act, like adding a tenant. The credentials themselves are in the
# vault as vault_mqtt_users; this file says only what each account may touch.
#
# Mosquitto denies anything not granted, so an account missing from this map
# can authenticate and then do nothing.
mqtt_acls:
  # lightd, in the eds tenant. Publishes scenes to any stand and reads their
  # presence; it never needs to write a stand's own status.
  lightd:
    - { access: write, topic: "eds/lightstand/+/scene" }
    - { access: read, topic: "eds/lightstand/+/status" }
    - { access: read, topic: "eds/lightstand/+/state" }
    - { access: write, topic: "eds/lightd/status" }

  # The LP jacket stand. Scoped to its OWN topics on purpose: a device
  # credential that could write any stand's scene could repaint every stand in
  # the house, and the IoT segment is rated Medium trust precisely because its
  # devices are assumed reachable by things we did not write.
  lp-stand-01:
    - { access: read, topic: "eds/lightstand/lp-stand-01/scene" }
    - { access: write, topic: "eds/lightstand/lp-stand-01/status" }
    - { access: write, topic: "eds/lightstand/lp-stand-01/state" }

# OpenBao, for the broker's TLS certificate (ADR-0016, CHG-0015).
#
# The broker serves TLS only, so without these it has no listener at all and
# the role refuses to start rather than come up serving nothing.
#
# Both are used on the CONTROL NODE: every OpenBao call in the vernemq role is
# delegated to localhost, so the messaging VM needs no path to OpenBao and
# none is opened for it. IoT Backend reaching the substrate's secret store
# would be a poor trade for a certificate that can be handed to it instead.
#
# groups['openbao'] rather than groups['tenant_dns'], which deevnet_api uses
# for the same value. They resolve to the same host today because PowerDNS and
# OpenBao share the identity VM, but only one of those names means "where the
# secret store is".
vernemq_openbao_addr: "https://{{ hostvars[groups['openbao'] | first].env.interfaces.eth0.ip }}:8200"
vernemq_openbao_ca_local: "{{ playbook_dir }}/../.openbao/{{ groups['openbao'] | first }}-listener.pem"

# Ansible's own AppRole, read across from the host that holds it.
#
# vault_openbao_ansible_role_id lives in group_vars/openbao/vault.yml, so it is
# visible only to members of the openbao group - and this host is not one, nor
# should it be. The role's default reads the bare variable, which is right for
# a host inside that group and empty everywhere else; reaching through hostvars
# is how deevnet_api solves the same problem, and it stays in inventory rather
# than the role so the role does not have to know what the group is called.
vernemq_openbao_ansible_role_id: "{{ hostvars[groups['openbao'] | first].vault_openbao_ansible_role_id | default('') }}"
vernemq_openbao_ansible_secret_id: "{{ hostvars[groups['openbao'] | first].vault_openbao_ansible_secret_id | default('') }}"

# Who may reach the broker's auth database (CHG-0016).
#
# The Deevnet API, and nothing else. Declaring this is what publishes the port
# at all - an empty list leaves the database reachable only by the broker over
# the private podman network.
#
# This is the enforcement the zone rule cannot express. `iot -> iot_backend` is
# a zone-level pass, so a port on this segment is reachable from every device
# on VLAN 30 whatever the narrow platform -> iot_backend rule says. The zone
# rule states the intent; firewalld on this host decides which port.
# See standards/network-segmentation "Reachability and Permission".
# HELD EMPTY 2026-09-20. Declaring a source publishes the port, and a published
# podman port is NOT filtered by the firewalld rich rule this role adds:
# publishing is DNAT plus forward, so it never reaches the INPUT chain the rule
# sits on, and netavark puts the container subnet in firewalld's trusted zone.
# Proven, not assumed: with the rule in place, 5432 answered from a host that
# is not an allowed source. Empty means the database keeps no published port
# and is reachable only by the broker. See CHG-0016.
vernemq_db_allowed_sources: []
